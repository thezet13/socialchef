{
  "name": "socialchef",
  "private": true,
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.16",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "sync:fonts": "node scripts/sync-fonts.mjs",
    "dev": "pnpm sync:fonts && pnpm -r dev",
    "build": "pnpm sync:fonts && pnpm -r build"
  }
}
