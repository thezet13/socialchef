import { Inter } from "next/font/google";

// примеры — добавляй только реально используемые
export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});
